import React from 'react';

import StyledSnakeGrid from "./StyledSnakeGrid";

const SnakeGrid = () => {

  return (
    <StyledSnakeGrid >
      I am SnakeGrid
    </StyledSnakeGrid>
  );
};

export default SnakeGrid;